[build]
buildCommand = "cd apps/backend && npm install && rm -rf dist && npm run build"

[deploy]
startCommand = "cd apps/backend && node dist/main"
healthcheckPath = "/api"
restartPolicyType = "on_failure"
